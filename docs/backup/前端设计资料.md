# LIS-RSS Literature Tracker - 前端设计资料

本文档为前端设计团队提供项目背景、设计约束和参考资料，用于指导界面重构升级工作。

---

## 一、项目概述

### 1.1 产品定位

**LIS-RSS Literature Tracker** 是一款面向研究人员和知识工作者的文献追踪工具，核心功能包括：

- **RSS 订阅聚合**：自动抓取和监控学术/技术 RSS 源
- **智能过滤**：基于主题领域和关键词的 LLM 智能文章筛选
- **语义搜索**：向量检索支持自然语言查询相关文献
- **知识管理**：文章标注、关键词生成、中文翻译、导出功能

### 1.2 目标用户画像

| 属性 | 描述 |
|------|------|
| 职业 | 研究生、博士生、研究员、技术专家 |
| 场景 | 日常文献阅读、学术研究、技术追踪 |
| 设备 | 主要桌面端，兼顾移动端 |
| 痛点 | 信息过载、筛选效率低、跨语言阅读困难 |
| 需求 | 高效筛选、语义检索、阅读辅助 |

### 1.3 核心价值主张

> **让研究者用自然语言从海量文献中精准发现价值内容**

---

## 二、当前设计分析

### 2.1 设计风格现状

**Academic Brutalist（学术 brutalist）风格**

| 维度 | 当前实现 |
|------|----------|
| 圆角 | 0px（直角边框） |
| 阴影 | 硬阴影 `6px 6px 0` 位移 |
| 颜色 | 学术蓝为主色（#1a4d7a） |
| 字体 | 衬线标题 + 无衬线正文 |
| 间距 | 黄金比例级数（4px, 8px, 12px, 16px...） |
| 动画 | 轻微 hover 效果 + fade-in-up 入场 |

### 2.2 设计 token 现状

#### 颜色系统

```css
/* 亮色模式 */
--accent-primary: #1a4d7a;      /* 学术蓝 */
--accent-hover: #2d6da8;        /* 悬停态 */
--accent-subtle: rgba(26, 77, 122, 0.08);

--green: #1a5c3a;               /* 通过/成功 */
--red: #b82a2a;                 /* 拒绝/错误 */
--amber: #b8712a;              /* 警告/中等 */

--bg-primary: #fafaf8;         /* 主背景 */
--bg-surface: #ffffff;         /* 卡片背景 */
--bg-elevated: #f5f5f0;        /* 高亮背景 */

--text-primary: #0a0a0a;        /* 主文字 */
--text-secondary: #404040;      /* 次要文字 */
--text-tertiary: #808080;       /* 辅助文字 */
```

#### 字体系统

```css
--font-heading: 'Crimson Pro', serif;      /* 标题 */
--font-body: 'IBM Plex Sans', sans-serif;   /* 正文 */
--font-mono: 'IBM Plex Mono', monospace;    /* 代码 */
--font-meta: 'Inter', system-ui;            /* UI 元素 */
```

#### 字号级数

```
10px → 12px → 14px → 16px → 20px → 24px → 28px → 32px+ (clamp)
```

### 2.3 页面结构现状

```
src/views/
├── layout.ejs          # 全局布局框架
├── index.ejs           # 首页（仪表盘 + 文章时间线）
├── articles.ejs        # 文章列表
├── article-detail.ejs  # 文章详情
├── search.ejs          # 语义搜索
├── settings.ejs        # 设置页（RSS源 + LLM配置）
├── topics.ejs          # 主题领域管理
├── filter-logs.ejs     # 过滤日志
├── filter-stats.ejs    # 过滤统计
├── login.ejs           # 登录页
└── error.ejs          # 错误页
```

---

## 三、页面详细说明

### 3.1 首页 (index.ejs)

**功能定位**：用户登录后的主入口，展示当日文献概览和最新通过筛选的文章。

**当前布局**：
```
┌─────────────────────────────────────────────┐
│ 站点标题栏                                   │
├─────────────────────────────────────────────┤
│                                             │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐      │
│  │今日新增│ │待处理 │ │已分析 │ │通过率 │      │  ← 统计卡片
│  └──────┘ └──────┘ └──────┘ └──────┘      │
│                                             │
│  ─────────────────────────────────────────  │
│                                             │
│  2024年1月15日                               │  ← 日期分组
│  ┌─────────────────────────────────────┐     │
│  │ 文章卡片                              │     │
│  └─────────────────────────────────────┘     │
│  ┌─────────────────────────────────────┐     │
│  │ 文章卡片                              │     │
│  └─────────────────────────────────────┘     │
│                                             │
│  2024年1月14日                               │  ← 日期分组
│  ┌─────────────────────────────────────┐     │
│  │ 文章卡片                              │     │
│  └─────────────────────────────────────┘     │
│                                             │
│  ─────────────────────────────────────────  │
│  ← 1 2 3 4 5 →                              │  ← 分页
└─────────────────────────────────────────────┘
```

**文章卡片内容**：
- 标题（链接到详情）
- 状态标签：通过/拒绝/待处理
- 来源名称
- 发布时间/相对时间
- 原文链接
- 摘要（可展开/收起）
- 关键词标签

**交互需求**：
- 卡片 hover 效果：位移 + 阴影变化
- 摘要展开/收起
- 分页浏览
- URL 参数保持分页状态

---

### 3.2 文章列表页 (articles.ejs)

**功能定位**：管理和浏览所有文章，支持多维度筛选和批量操作。

**当前布局**：
```
┌─────────────────────────────────────────────┐
│ 文章列表                         [批量处理] │
├─────────────────────────────────────────────┤
│ [来源 ▼] [状态 ▼] [处理状态 ▼] [搜索框 🔍] │  ← 筛选栏
├─────────────────────────────────────────────┤
│  显示 1-20 / 共 1000 篇文章                   │  ← 结果计数
├─────────────────────────────────────────────┤
│  ┌─────────────────────────────────────┐     │
│  │ 文章卡片                              │     │
│  │ - 标题、状态、来源、处理状态、原文链接 │     │
│  │ - 摘要                                │     │
│  │ - 关键词、批量操作按钮                │     │
│  └─────────────────────────────────────┘     │
│  ┌─────────────────────────────────────┐     │
│  │ 文章卡片                              │     │
│  └─────────────────────────────────────┘     │
│                                             │
│  ─────────────────────────────────────────  │
│  ← 1 2 3 4 5 →                              │
└─────────────────────────────────────────────┘
```

**筛选维度**：
- RSS 来源（多选）
- 过滤状态：通过/拒绝/待处理
- 处理状态：待处理/处理中/已完成/失败
- 关键词搜索（标题/摘要）

**批量操作**：
- 批量处理待处理文章

---

### 3.3 文章详情页 (article-detail.ejs)

**功能定位**：展示单篇文章的完整信息，提供阅读和导出功能。

**当前布局**：
```
┌─────────────────────────────────────┬─────────────────┐
│                                     │                 │
│  ← 返回列表                         │  [操作卡片]     │
│                                     │  - 查看原文     │
│  文章标题                            │  - 导出 Markdown│
│  来源 · 链接 · 发布时间              │  - 重新处理     │
│                                     │  - 删除文章     │
│  ───────────────────────────────────│                 │
│                                     │  [相关文章]     │
│  RSS 摘要                            │  - 相关文章列表 │
│  （如有）                            │                 │
│                                     │  [关键词]       │
│  ───────────────────────────────────│  - #tag1 #tag2  │
│                                     │                 │
│  中文翻译                            │  [文章信息]     │
│  （如有）                            │  - 过滤状态     │
│                                     │  - 处理状态     │
│  ───────────────────────────────────│  - 发布时间     │
│                                     │  - 抓取时间     │
│  过滤匹配                            │                 │
│  - 【领域】关键词... 原因            │                 │
│                                     │                 │
│  ───────────────────────────────────│                 │
│                                     │                 │
│  RSS 正文                            │                 │
│  （Markdown 渲染）                   │                 │
│                                     │                 │
│  ───────────────────────────────────│                 │
│                                     │                 │
│  抓取全文 [如有，与RSS正文不同]       │                 │
│  （Markdown 渲染）                   │                 │
│                                     │                 │
└─────────────────────────────────────┴─────────────────┘
```

**侧边栏组件**：
- **操作按钮**：查看原文、导出 Markdown、重新处理、删除
- **相关文章**：语义检索相关度 > 70% 的文章
- **关键词**：LLM 生成的标签
- **文章元数据**：过滤状态、处理状态、时间信息

---

### 3.4 语义搜索页 (search.ejs)

**功能定位**：支持自然语言查询，精准发现相关文献。

**当前布局**：
```
┌─────────────────────────────────────────────┐
│                                             │
│              语义搜索                        │
│                                             │
│   ┌─────────────────────────────────────┐   │
│   │  [输入问题或关键词...              ]│   │
│   │                        [搜索]        │   │
│   └─────────────────────────────────────┘   │
│   提示: 尝试 "LLM 在代码生成中的应用"       │
│                                             │
├─────────────────────────────────────────────┤
│                                             │
│  找到 50 篇相关文章    搜索 "xxx"           │
│                                             │
│  ┌─────────────────────────────────────┐     │
│  │  文章标题                    [相关度 92%]│   │
│  │  来源 · 2小时前 · 原文链接             │     │
│  │  摘要摘录...                          │     │
│  │  #tag1 #tag2                          │     │
│  └─────────────────────────────────────┘     │
│  ┌─────────────────────────────────────┐     │
│  │  文章标题                    [相关度 85%]│   │
│  │  ...                                  │     │
│  └─────────────────────────────────────┘     │
│                                             │
│  ─────────────────────────────────────────  │
│  ← 1 2 3 4 5 →                              │
└─────────────────────────────────────────────┘
```

**搜索模式**：
- 语义搜索：向量检索，自然语言查询
- 关键词搜索：SQLite LIKE，AND+OR 组合
- 混合搜索：语义 + 关键词融合

**结果展示**：
- 相关度百分比（高/中/低）
- 高亮匹配关键词
- 分页浏览

---

### 3.5 设置页 (settings.ejs)

**功能定位**：系统配置管理，包含 RSS 源管理和 LLM 配置。

**当前布局**：
```
┌─────────────────────────────────────────────┐
│                                             │
│ RSS 订阅源                        [+ 添加]   │
│ ┌─────────────────────────────────────────┐ │
│ │ 名称     │ URL           │ 状态 │ 操作  │ │
│ ├─────────────────────────────────────────┤ │
│ │ ArXiv   │ arxiv.org/... │ 启用 │ 编辑  │ │
│ │ ...     │ ...           │ ...  │ ...   │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ ─────────────────────────────────────────  │
│                                             │
│ LLM 配置                          [+ 添加]   │
│ ┌─────────────────────────────────────────┐ │
│ │ 提供商  │ 类型   │ 模型    │ API  │ 状态│ │
│ ├─────────────────────────────────────────┤ │
│ │ OpenAI  │ LLM    │ gpt-4o │ **** │ -   │ │
│ │ ...     │ ...    │ ...    │ ...  │ ... │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ ─────────────────────────────────────────  │
│                                             │
│ Chroma 设置                                 │
│ [Host] [Port] [Collection] [距离度量 ▼]     │
│                              [保存]          │
│                                             │
└─────────────────────────────────────────────┘
```

**RSS 源管理功能**：
- 添加/编辑/删除 RSS 源
- URL 验证（实时验证）
- 抓取间隔设置
- 启用/禁用状态
- 手动触发抓取

**LLM 配置功能**：
- 多配置管理（LLM / Embedding / Rerank）
- 提供商选择（OpenAI / Gemini / 自定义）
- 连接测试
- 设为默认

---

### 3.6 主题领域管理页 (topics.ejs)

**功能定位**：配置过滤规则，定义感兴趣的研究领域和关键词。

**当前布局**：
```
┌─────────────────────────────┬─────────────────────────────┐
│                             │                             │
│ 主题领域          [+ 添加]  │  选择一个领域查看关键词      │
│                             │                             │
│ 名称      │ 关键词 │ 状态 │  ┌───────────────────────┐  │
│ ├──────────────────────────│  │                       │  │
│ │ 机器学习 │ 12    │ 启用 │  │ (无关键词占位图)      │  │
│ │ 深度学习 │ 8     │ 启用 │  │                       │  │
│ │ NLP      │ 5     │ 禁用 │  └───────────────────────┘  │
│ │ ...      │ ...   │ ...  │                             │
│ └──────────────────────────┘                             │
│                             │                             │
│                             │  [+ 添加关键词]             │
│                             │                             │
│                             │  关键词   │ 权重 │ 操作   │
│                             │  ├───────────────────────│ │
│                             │  │ 深度学习 │ 1.5  │ 编辑  │ │
│                             │  │ Transformer│ 2.0 │ 编辑  │ │
│                             │  │ ...      │ ...  │ ...   │ │
│                             │  └───────────────────────│ │
└─────────────────────────────┴─────────────────────────────┘
```

**领域配置**：
- 领域名称
- 领域描述（语义匹配参考）
- 优先级（数值越大优先级越高）
- 启用/禁用状态

**关键词配置**：
- 关键词
- 描述（语义匹配参考）
- 权重（0-10，数值越大权重越高）
- 启用/禁用状态

---

### 3.7 过滤日志页 (filter-logs.ejs)

**功能定位**：查看历史过滤记录，理解筛选决策过程。

**当前布局**：
```
┌─────────────────────────────────────────────┐
│ 过滤日志                                    │
├─────────────────────────────────────────────┤
│ [全部领域 ▼] [全部结果 ▼]                   │
├─────────────────────────────────────────────┤
│ ┌─┬──────────┬──────┬────┬──────┬────┬────┐ │
│ │ │ 文章标题 │ 领域 │结果│分数  │匹配│原因│ │
│ ├─┼──────────┼──────┼────┼──────┼────┼────┤ │
│ │▶│ ...     │机器学习│通过│0.92 │...│...│ │
│ ├─┼──────────┼──────┼────┼──────┼────┼────┤ │
│ │▶│ ...     │NLP   │拒绝│0.45 │...│...│ │
│ └─┴──────────┴──────┴────┴──────┴────┴────┘ │
│                                             │
│  [详情展开行 - 显示 LLM 响应 JSON]           │
│                                             │
│  ─────────────────────────────────────────  │
│  第 1 页，共 50 页  [上一页] [下一页]        │
└─────────────────────────────────────────────┘
```

**日志字段**：
- 文章标题（链接到详情）
- 匹配领域
- 通过/拒绝结果
- 相关性分数
- 匹配关键词
- 过滤原因
- 时间戳
- LLM 完整响应（展开查看）

---

### 3.8 过滤统计页 (filter-stats.ejs)

**功能定位**：可视化展示过滤效果数据。

**当前布局**：
```
┌─────────────────────────────────────────────┐
│ 过滤统计                                    │
├─────────────────────────────────────────────┤
│ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐│
│ │总过滤  │ │通过    │ │拒绝    │ │通过率  ││
│ │1,234   │ │892     │ │342     │ │72.3%  ││
│ └────────┘ └────────┘ └────────┘ └────────┘│
│ ┌────────┐                                  │
│ │平均分数│                                  │
│ │0.78    │                                  │
│ └────────┘                                  │
├─────────────────────────────────────────────┤
│ 各领域统计                                   │
│ ┌────────────┬──────┬─────┬──────┬──────┐ │
│ │ 领域      │过滤数 │通过 │通过率│平均分│ │
│ ├────────────┼──────┼─────┼──────┼──────┤ │
│ │ 机器学习   │ 500  │380  │76%   │0.82  │ │
│ │ NLP        │ 200  │150  │75%   │0.79  │ │
│ │ ...        │ ...  │...  │...   │...   │ │
│ └────────────┴──────┴─────┴──────┴──────┘ │
│                                             │
│ ─────────────────────────────────────────  │
│ 分数分布图表 [待实现]                       │
└─────────────────────────────────────────────┘
```

---

### 3.9 登录页 (login.ejs)

**功能定位**：用户认证入口。

**当前布局**：
```
┌─────────────────────────────────────────────┐
│                                             │
│                                             │
│         📚 LIS-RSS Literature Tracker           │
│                                             │
│     ┌─────────────────────────────────┐     │
│     │ 用户名                          │     │
│     │ 密码                            │     │
│     │ [登 录]                         │     │
│     │ [错误信息]                      │     │
│     └─────────────────────────────────┘     │
│                                             │
│     默认用户: admin / 密码: admin123         │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 四、数据结构

### 4.1 文章数据模型

```typescript
interface Article {
  id: number;
  rss_source_id: number;
  rss_source_name: string;
  title: string;
  url: string;
  summary: string;           // RSS 原始摘要
  content: string;            // RSS 正文（Markdown）
  markdown_content: string;   // 抓取全文（可能覆盖）
  filter_status: 'pending' | 'passed' | 'rejected';
  filter_score: number;
  process_status: 'pending' | 'processing' | 'completed' | 'failed';
  keywords: string[];         // LLM 生成
  translation?: {
    title_zh: string;
    summary_zh: string;
  };
  published_at: string;
  created_at: string;
}
```

### 4.2 RSS 源数据模型

```typescript
interface RSSSource {
  id: number;
  name: string;
  url: string;
  status: 'active' | 'inactive';
  fetch_interval: number;     // 秒
  last_fetched_at: string;
  article_count?: number;
}
```

### 4.3 主题领域数据模型

```typescript
interface TopicDomain {
  id: number;
  name: string;
  description: string;
  is_active: boolean;
  priority: number;          // 0-100
  keyword_count: number;
}
```

### 4.4 关键词数据模型

```typescript
interface TopicKeyword {
  id: number;
  domain_id: number;
  keyword: string;
  description: string;
  weight: number;            // 0-10
  is_active: boolean;
}
```

### 4.5 搜索结果数据模型

```typescript
interface SearchResult {
  id: number;
  title: string;
  url: string;
  summary: string;
  excerpt: string;          // 搜索匹配片段
  rss_source_name: string;
  published_at: string;
  relevance: number;         // 0-1
  score: number;             // 综合分数
  tags: string;              // 逗号分隔
}
```

### 4.6 统计数据模型

```typescript
interface FilterStats {
  totalFiltered: number;
  totalPassed: number;
  totalRejected: number;
  passRate: number;
  avgRelevanceScore: number;
  byDomain: Array<{
    domainName: string;
    totalFiltered: number;
    totalPassed: number;
    passRate: number;
    avgScore: number;
  }>;
}
```

---

## 五、交互需求

### 5.1 全局交互

| 交互 | 当前实现 | 建议优化 |
|------|----------|----------|
| 页面加载 | 简单 loading 文本 | skeleton 骨架屏 |
| 状态反馈 | alert 弹窗 | toast 通知 |
| 表单提交 | 页面刷新 | AJAX + loading 状态 |
| 错误展示 | 错误页跳转 | 原地错误提示 |

### 5.2 列表交互

| 交互 | 当前实现 | 建议优化 |
|------|----------|----------|
| 分页 | URL 参数 + 刷新 | 无刷新加载 |
| 筛选 | 下拉框 + 刷新 | 即时筛选 |
| 排序 | 无 | 列头可排序 |
| 批量操作 | 按钮点击 | checkbox 多选 |

### 5.3 搜索交互

| 交互 | 当前实现 | 建议优化 |
|------|----------|----------|
| 输入 | 手动点击搜索 | 实时搜索（debounce） |
| 搜索建议 | 无 | 搜索历史/热门词 |
| 结果高亮 | mark 标签 | 更好的视觉区分 |
| 相关度展示 | 百分比标签 | 可视化进度条 |

### 5.4 动画效果

```css
/* 当前动画 */
@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

/* 建议增强 */
.fade-in-up { animation: fadeInUp 0.4s ease-out; }

/* 建议添加 */
.card-hover { transition: transform 0.2s, box-shadow 0.2s; }
.card-hover:hover { transform: translateY(-4px); }
.modal-enter { animation: modalIn 0.3s ease-out; }
.toast-enter { animation: toastIn 0.3s ease-out; }
```

---

## 六、技术约束

### 6.1 技术栈

| 类别 | 技术 |
|------|------|
| 模板引擎 | EJS |
| 样式 | CSS（原生 + CSS Variables） |
| 脚本 | 原生 JavaScript（ES6+） |
| 构建 | 纯 CSS 文件 |
| 兼容性 | 现代浏览器（Chrome/Firefox/Safari/Edge） |

### 6.2 性能约束

| 指标 | 目标值 |
|------|--------|
| 首屏加载 | < 1s |
| 页面切换 | < 300ms |
| API 响应 | < 2s |
| 列表渲染 | < 500ms（100 条） |
| 搜索响应 | < 1s |

### 6.3 可访问性

| 要求 | 说明 |
|------|------|
| 键盘导航 | 支持 Tab / Enter / Escape |
| 屏幕阅读 | 语义化 HTML / ARIA 标签 |
| 颜色对比 | WCAG AA 级（4.5:1） |
| 响应式 | 768px 断点 |

---

## 七、设计升级建议

### 7.1 设计方向参考

**方案 A：现代学术风格**
- 保留部分 brutalist 元素（硬边框、学术字体）
- 引入更柔和的配色和微圆角
- 增强信息层级和阅读体验

**方案 B：极简工具风格**
- 完全现代化 UI
- 强调内容本身，减少视觉噪音
- 适合长时间阅读和工作

**方案 C：数据密集型**
- 强化数据可视化
- 更好的表格和统计展示
- 适合专业研究人员

### 7.2 关键改进点

| 优先级 | 页面 | 改进方向 |
|--------|------|----------|
| P0 | 首页 | 统计卡片视觉增强、时间线交互 |
| P0 | 搜索页 | 搜索体验、结果展示、相关度可视化 |
| P1 | 详情页 | 阅读体验优化、侧边栏信息整合 |
| P1 | 设置页 | 配置管理界面优化、表单体验 |
| P2 | 统计页 | 图表可视化 |
| P2 | 登录页 | 品牌感提升 |

### 7.3 建议组件库

如需引入组件库，建议考虑：

| 组件库 | 特点 | 适合度 |
|--------|------|--------|
| Radix UI | 无样式、可访问性好 | ⭐⭐⭐⭐⭐ |
| shadcn/ui | 现代、可定制 | ⭐⭐⭐⭐⭐ |
| Ark UI | Headless、框架无关 | ⭐⭐⭐⭐ |
| Kobalte | SolidJS 生态 | ⭐⭐⭐ |

---

## 八、交付物清单

设计团队需要提供：

### 8.1 设计稿

- [ ] 首页（桌面端 + 移动端）
- [ ] 文章列表页
- [ ] 文章详情页
- [ ] 搜索页
- [ ] 设置页（RSS 源 + LLM 配置）
- [ ] 主题管理页
- [ ] 过滤日志页
- [ ] 过滤统计页
- [ ] 登录页
- [ ] 错误页

### 8.2 设计规范

- [ ] 颜色系统（主色、辅助色、功能色）
- [ ] 字体系统（中英文字体、字号级数）
- [ ] 间距系统
- [ ] 圆角/阴影规范
- [ ] 图标系统
- [ ] 动效规范

### 8.3 设计资源

- [ ] 图标资源（SVG）
- [ ] 字体文件
- [ ] 组件设计（按钮、表单、卡片等）

---

## 九、参考链接

### 9.1 产品参考

| 产品 | 特点 | 参考价值 |
|------|------|----------|
| arXiv.org | 学术论文检索 | 信息密度、筛选功能 |
| Semantic Scholar | 学术搜索 | 相关文献推荐 |
| Feedly | RSS 阅读 | RSS 管理、阅读体验 |
| Notion | 知识管理 | 界面设计、交互模式 |

### 9.2 设计参考

| 资源 | 类型 |
|------|------|
| Dribbble - SaaS Dashboards | 管理后台设计 |
| Awwward - Productivity | 生产力工具 |
| Pinterest - Academic UI | 学术界面 |

---

## 十、联系方式

如有疑问，请联系开发团队获取：

1. API 接口文档
2. 完整的数据库 schema
3. 部署环境访问
4. 用户测试账号

---

*文档版本：1.0*  
*最后更新：2024年*
